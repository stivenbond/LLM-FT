# 🇦🇱 Lahuta Project Roadmap: From Foundation to Production

This document outlines the sequential steps required to complete the **Lahuta Model Ecosystem**. The project has recently transitioned to a modular, multi-model architecture.

## 🟢 Phase 1: Infrastructure Synchronization & Cleanup
*Target: Align documentation and scripts with the new modular structure.*

- [x] **Update README.md**: Rewrite the root README to reflect the modular `models/` architecture and the dynamic `api/` server.
- [ ] **Migrate Pipeline.ipynb**: Update the Colab notebook to use model-specific paths (e.g., `models/albanian_analysis/scripts/...` instead of `scripts/...`). *[Requires manual update of .ipynb]*
- [x] **Update requirements.txt**: Ensure all dependencies for both the API (`fastapi`, `uvicorn`, `llama-cpp-python`) and the pipelines (`anthropic`, `openai`, `jsonschema`, `scikit-learn`) are present.
- [x] **Verify .env implementation**: Ensure all scripts use `load_dotenv()` correctly to pull keys for Groq, Anthropic, and OpenAI.

## 🟡 Phase 2: Model Feature Parity
*Target: Ensure all models in `models/` have the same functional capabilities.*

- [x] **Complete `teacher_diary_generator`**:
    - [x] Implement `output_validator.py` in `models/teacher_diary_generator/scripts/`.
    - [x] Define the specific `training_example_v1.json` schema for diary entries.
    - [x] Create specialized prompt templates in `models/teacher_diary_generator/prompts/`.
- [x] **Standardize `run_pipeline.py`**: Ensure the master pipeline script in each model directory is consistent and supports the same flags (`--augment`, `--split`, etc.).
- [x] **Validation Suite**: Add `tests/` for the `teacher_diary_generator` to verify its schema validation and prompt building.

## 🟠 Phase 3: Data Production (The "Great Generation")
*Target: Generate a high-quality dataset of at least 5k-10k examples per model.*

- [ ] **Seed Expansion**: Collect more high-quality Albanian seed articles for the `albanian_analysis` model across all 4 registers.
- [ ] **Synthetic Generation**: Run `generate_synthetic.py` using Claude 3.5 Sonnet or GPT-4o to create "Gold" JSON scaffolds.
- [ ] **Programmatic Augmentation**: Run `augment.py` to inject grammatical errors and style inconsistencies to create "Correction" pairs.
- [ ] **Final Stratification**: Run `split_dataset.py` to produce `train.jsonl`, `val.jsonl`, and `test.jsonl` files.

## 🔴 Phase 4: Training & Fine-Tuning
*Target: Train the Gemma 4:e4b base model on the generated datasets.*

- [ ] **Prepare Training Configs**: Create YAML configs for `axolotl` or `unsloth` in `models/[model_name]/training/`.
- [ ] **CoT Fine-Tuning**: Run SFT (Supervised Fine-Tuning) specifically targeting the `<think>` block structure for Chain-of-Thought reasoning.
- [ ] **Quantization**: Convert fine-tuned weights to GGUF format (Q4_K_M or Q8_0) for use with `llama-cpp-python`.
- [ ] **HF Upload**: Automate the upload of new model versions to the HuggingFace Hub.

## 🔵 Phase 5: Evaluation & RLHF (The Feedback Loop)
*Target: Quantify model performance and implement human-in-the-loop improvements.*

- [ ] **Automated Evals**: Run the scripts in `evals/` to calculate:
    - [ ] JSON Validity Rate.
    - [ ] Metric Accuracy (MAE for scores).
    - [ ] Linguistic Quality (using a stronger LLM as a judge).
- [ ] **RLHF Collection**: Integrate the `POST /feedback` endpoint into a simple internal UI to collect correction pairs.
- [ ] **DPO Preparation**: Create a script to convert collected feedback into DPO (Direct Preference Optimization) datasets.

## 🚀 Phase 6: Deployment & Ecosystem
*Target: Make Lahuta accessible and robust for production use.*

- [ ] **Dockerization**: Create a `Dockerfile` that packages the API and optionally pre-downloads the default models.
- [ ] **Frontend/PWA**: Build a sleek, modern UI (Next.js/Vite) to interact with the models, supporting streaming and feedback.
- [ ] **API Documentation**: Generate and host ReDoc/Swagger documentation for the API endpoints.
- [ ] **CI/CD Pipeline**: Setup GitHub Actions to run tests and validate schemas on every push.

---
*Generated by Antigravity on April 29, 2026*
